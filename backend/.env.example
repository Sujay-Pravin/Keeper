# Server Configuration
PORT=5000
FRONTEND_URL=http://localhost:5173

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_DATABASE=api_vault

# JWT Secret - Change this in production!
JWT_SECRET=your_super_secure_jwt_secret_key_change_me
JWT_EXPIRES_IN=1d

# Encryption Key for API Keys (32 characters for AES-256)
# Generate a secure key with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ENCRYPTION_KEY=replace_with_32_character_secure_key
